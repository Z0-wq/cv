# 下载的预训练模型权重（体积大，且可通过代码自动重新下载，不需要占用仓库空间）
*.pt
*.onnx
*.engine

# 本地截图/笔记附件（个人留存，非项目内容）
note.assets/

# 数据集（体积大，README里写清楚下载方式即可，不直接上传）
data/
datasets/

# Python生成的临时文件
__pycache__/
*.pyc

# 无实际内容的临时测试文件
test.py

# 内部工作流工具目录，与项目内容无关
.spec-workflow/
